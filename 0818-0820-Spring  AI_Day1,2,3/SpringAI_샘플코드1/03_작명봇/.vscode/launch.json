{
  // F5 로 실행·디버그한다. 키는 여기(실행 설정)에 두면 소스에 커밋되지 않는다.
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "3장 실습 · 첫 AI 호출과 고장 재현",
      "request": "launch",
      "mainClass": "com.skala.lab2.LabApplication",
      "projectName": "03_작명봇",
      "vmArgs": "-Dfile.encoding=UTF-8",
      "env": { "OPENAI_API_KEY": "${env:OPENAI_API_KEY}" }
    }
  ]
}
