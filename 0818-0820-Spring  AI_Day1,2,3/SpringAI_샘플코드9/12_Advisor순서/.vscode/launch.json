{
  // F5 로 실행·디버그한다. 키는 여기(실행 설정)에 두면 소스에 커밋되지 않는다.
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "12장 실습 · Advisor 순서가 곧 정책",
      "request": "launch",
      "mainClass": "com.skala.lab12.LabApplication",
      "projectName": "12_Advisor순서",
      "vmArgs": "-Dfile.encoding=UTF-8",
      "env": { "OPENAI_API_KEY": "${env:OPENAI_API_KEY}" }
    }
  ]
}
