{
  // F5 로 실행·디버그한다. 키는 여기(실행 설정)에 두면 소스에 커밋되지 않는다.
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "10장 실습 · Tool Calling",
      "request": "launch",
      "mainClass": "com.skala.lab10.LabApplication",
      "projectName": "10_점심추천도구",
      "vmArgs": "-Dfile.encoding=UTF-8",
      "env": { "OPENAI_API_KEY": "${env:OPENAI_API_KEY}" }
    }
  ]
}
