{
  // F5 로 실행·디버그한다. 키는 여기(실행 설정)에 두면 소스에 커밋되지 않는다.
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "11장 실습 · 승인 게이트와 감사 로깅",
      "request": "launch",
      "mainClass": "com.skala.lab11.LabApplication",
      "projectName": "11_승인게이트",
      "vmArgs": "-Dfile.encoding=UTF-8",
      "env": { "OPENAI_API_KEY": "${env:OPENAI_API_KEY}" }
    }
  ]
}
