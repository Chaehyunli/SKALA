{
  // F5 로 실행·디버그한다. 키는 여기(실행 설정)에 두면 소스에 커밋되지 않는다.
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "13장 실습 · 구간 계측 · 캐시 · 폴백",
      "request": "launch",
      "mainClass": "com.skala.lab13.LabApplication",
      "projectName": "13_병목과캐시",
      "vmArgs": "-Dfile.encoding=UTF-8",
      "env": { "OPENAI_API_KEY": "${env:OPENAI_API_KEY}" }
    }
  ]
}
